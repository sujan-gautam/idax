{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "npm install && npm run generate && (if [ -n \"$DATABASE_URL\" ]; then npm run db:push; else echo 'Skipping db:push: DATABASE_URL not found'; fi) && npm run build:packages && npm run build:web && npm run build:api"
    },
    "deploy": {
        "startCommand": "npm start -w @project-ida/unified-api",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}