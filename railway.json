{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "npm install --include=dev && npm run generate && npm run build:packages && npm run build:web && npm run build:api"
    },
    "deploy": {
        "startCommand": "npm run db:push && npm start -w @project-ida/unified-api",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}